import styled from 'styled-components';

export const BodyWrapper = styled.div`
	
`;

export const Total = styled.p`
  ${({ theme }) => theme.font.c_medium[3]};
  color: ${({ theme }) => theme.color.greyscale[18]};
`;