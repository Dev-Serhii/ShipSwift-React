import styled from 'styled-components';

export const BodyWrapper = styled.div`
	
`;

export const EditorOverloadStyle = styled.div`
  // Hides full screen button (glitch effect with modal)
  ._se_command_fullScreen {
    display: none;
  }
`;
